// import { StoryblokCMS } from "./cms"

// export async function storyblokLoader(slug: string) {
//     try {
//       const story = await StoryblokCMS.getStory({ slug: [slug] });
//       console.log("story", story);
//       return { story };
//     } catch (error) {
//       console.error(`Error loading page with slug "${slug}":`, error);
//       throw new Response("Page not found", { status: 404 });
//     }
//   }
  